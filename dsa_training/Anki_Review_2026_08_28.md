# Anki Review 2026-08-28

- Reviewed Kth Largest Element (Min-Heap).
- Reviewed Max-Heap for K Closest Points.
- Time Complexity: O(N log k) instead of O(N log N).
- Remember to push `(-dist, x, y)` into python's `heapq` for max-heap behavior.
