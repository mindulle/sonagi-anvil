# Anki Review - 2026-08-15

## Spaced Repetition (10 mins)

Reviewed the following concepts today:
- Trie (Prefix Tree) implementation: `children` dict vs array, `is_end_of_word` flag.
- Time complexities for Trie: insert `O(m)`, search `O(m)`, startsWith `O(m)`.
- Difference between `search` and `startsWith`: `search` requires `is_end_of_word` to be true.
