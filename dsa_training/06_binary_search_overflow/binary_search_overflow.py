class Solution:
    # TODO: Implement Binary Search avoiding the (left + right) // 2 formula
    # Use the safe formula: left + (right - left) // 2
    def search(self, nums: list[int], target: int) -> int:
        pass
