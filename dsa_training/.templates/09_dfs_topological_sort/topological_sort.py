class Solution:
    # TODO: Detect cycles in a directed graph using DFS.
    # You MUST track 3 states to avoid TLE: 0 (Unvisited), 1 (Visiting), 2 (Visited/Safe).
    def canFinish(self, numCourses: int, prerequisites: list[list[int]]) -> bool:
        pass
