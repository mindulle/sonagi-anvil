# Anki Review 2026-09-03

## Completed Reviews
- **House Robber (DP)**: Reviewed the recurrence relation `dp[i] = max(dp[i-1], dp[i-2] + nums[i])` optimized to O(1) space with two variables `rob1` and `rob2`.
- **Word Break (DP/Trie)**: Reviewed the bottom-up DP approach from yesterday's problem.

## Notes
- Space optimization in 1D DP problems is a very common pattern. Always check if only the last 1 or 2 states are needed.
