const ASSESSMENT_QUESTIONS = [
  {
    "id": "022_rotate_array",
    "prompt": "\"Given an integer array nums, rotate the array to the right by k steps, where k is non-negative. Do not return anything, modify nums in-place instead. Aim for O(1) extra space.\"",
    "code": "class Solution:\n    def rotate(self, nums: List[int], k: int) -> None:\n        n = len(nums)\n        k %= n\n        # Using slicing creates a new array of size n, which is O(n) space\n        nums[:] = nums[n-k:] + nums[:n-k]",
    "solution_html": "<h3>1. Space Complexity</h3><br><strong>문제점:</strong> 슬라이싱(<code>nums[n-k:] + nums[:n-k]</code>)은 새로운 배열을 생성하므로 $O(n)$ 공간을 사용합니다. 문제의 의도가 $O(1)$ 추가 공간 사용이라면 이 방식은 개선이 필요합니다.<br><strong>수정:</strong> <strong>Reversal Approach(역순 알고리즘)</strong>를 사용합니다.<br>1. 전체 배열을 뒤집습니다.<br>2. 처음 k개 요소를 뒤집습니다.<br>3. 나머지 n-k개 요소를 뒤집습니다.<br>이 방식은 추가 공간 없이 $O(1)$ 공간 복잡도로 해결할 수 있습니다."
  },
  {
    "id": "025_mutable_default_argument",
    "prompt": "Appends a value to a list. If no list is provided, it should create a new one.\nHowever, the current implementation has a bug related to mutable default arguments.\nFix the implementation.",
    "code": "class Solution:\n    def append_to_list(self, val, lst=[]):\n        lst.append(val)\n        return lst",
    "solution_html": "<code></code>`python<br>class Solution:<br>    def append_to_list(self, val, lst=None):<br>        if lst is None:<br>            lst = []<br>        lst.append(val)<br>        return lst<br><code></code>`"
  },
  {
    "id": "023_linked_list_cycle",
    "prompt": "\"Given head, the head of a linked list, determine if the linked list has a cycle in it. Aim for O(1) space.\"",
    "code": "def hasCycle(head: ListNode) -> bool:\n    visited = set()\n    current = head\n    while current:\n        if current in visited:\n            return True\n        visited.add(current)\n        current = current.next\n    return False",
    "solution_html": "<h3>1. Space Complexity</h3><br><strong>문제점:</strong> 해시셋(<code>visited</code>)을 사용하여 방문한 노드를 저장하면 노드의 개수만큼 메모리가 사용되므로 공간 복잡도가 $O(n)$이 됩니다.<br><strong>수정:</strong> <strong>Floyd's Cycle-Finding Algorithm (Tortoise and Hare)</strong>을 사용합니다.<br>- <code>slow</code> 포인터와 <code>fast</code> 포인터를 둡니다.<br>- <code>slow</code>는 한 칸씩, <code>fast</code>는 두 칸씩 이동합니다.<br>- 사이클이 있다면 <code>fast</code>가 <code>slow</code>를 따라잡게 됩니다.<br>이 방식은 추가적인 자료구조를 사용하지 않아 공간 복잡도가 $O(1)$입니다."
  },
  {
    "id": "030_longest_increasing_subsequence",
    "prompt": "Given an integer array `nums`, return the length of the longest strictly increasing subsequence.",
    "code": "def lengthOfLIS(nums: list[int]) -> int:\n    if not nums:\n        return 0\n    \n    dp = [0] * len(nums)\n    for i in range(len(nums)):\n        for j in range(i):\n            if nums[j] < nums[i]:\n                dp[i] = max(dp[i], dp[j] + 1)\n    \n    return max(dp)",
    "solution_html": "<h3>1. Logic Bug (치명적인 논리 오류)</h3><br><strong>문제점:</strong> <code>dp</code> 배열을 <code>1</code>로 초기화해야 합니다. 현재 코드는 <code>0</code>으로 초기화되어 있어, 아무것도 증가하지 않는 경우(본인만 있는 경우) 길이를 0으로 반환하는 버그가 있습니다.<br><br><h3>2. Complexity (시간 복잡도)</h3><br><strong>현재:</strong> 이중 for문을 사용하므로 <strong>O(N²)</strong> Time Complexity를 가집니다.<br><strong>최적화:</strong> <code>bisect</code> 모듈을 사용한 <strong>O(N log N)</strong> 알고리즘이 가능합니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The model's code has a logic bug: it initializes the <code>dp</code> array with <code>0</code> instead of <code>1</code>. This incorrectly reports the length as <code>0</code> for sequences of length 1. It should be initialized with <code>1</code> because every element is a subsequence of length 1.<br>Additionally, the solution uses an O(N²) approach, which can be optimized to O(N log N) using binary search (patience sorting).\""
  },
  {
    "id": "028_dp_vs_greedy",
    "prompt": "Find the minimum number of coins that make up the given amount.\nA greedy approach may not work, so use Dynamic Programming.",
    "code": "class Solution:\n    def coinChange(self, coins: List[int], amount: int) -> int:\n        coins.sort(reverse=True)\n        count = 0\n        for coin in coins:\n            count += amount // coin\n            amount %= coin\n        return count if amount == 0 else -1",
    "solution_html": "<code></code>`python<br>class Solution:<br>    def coinChange(self, coins: List[int], amount: int) -> int:<br>        dp = [float('inf')] * (amount + 1)<br>        dp[0] = 0<br>        for coin in coins:<br>            for i in range(coin, amount + 1):<br>                dp[i] = min(dp[i], dp[i - coin] + 1)<br>        return dp[amount] if dp[amount] != float('inf') else -1<br><code></code>`"
  },
  {
    "id": "016_sysdesign_url_shortener",
    "prompt": "\"초당 1,000개의 URL을 단축해야 하는 URL Shortener 서비스(예: bit.ly)의 아키텍처를 설계해줘. 데이터베이스 선택과 핵심 단축 알고리즘을 포함해야 해.\"",
    "code": "(모델의 답변 요약)\n\"데이터베이스로 관계형 DB(MySQL)를 사용하고, 사용자가 긴 URL을 입력하면 MD5 해시 함수를 사용하여 URL을 해싱한 뒤 앞 7자리를 잘라서 단축 URL 키로 사용합니다. 충돌이 발생하면 다시 해싱합니다.\"",
    "solution_html": "<h3>1. Algorithm Flaw (Hash Collision & Performance)</h3><br><strong>문제점:</strong> MD5 해시의 앞 7자리를 자르는 방식은 충돌(Collision) 확률이 높습니다. 충돌 시마다 DB에 쿼리를 던져 확인하고 다시 해싱하는 구조는 초당 1,000개의 쓰기 요청(TPS 1000)을 처리할 때 막대한 DB 부하를 유발합니다.<br><strong>수정:</strong> <code>Base62</code> 인코딩과 분산 ID 생성기(예: Twitter Snowflake, 혹은 RDBMS의 Auto-increment ID를 활용한 Base62 변환), 또는 사전에 키를 생성해두는 Key Generation Service (KGS)와 ZooKeeper 조합을 제안하는 것이 모범 답안입니다.<br><br><h3>2. Database Choice (Read/Write Heavy)</h3><br><strong>문제점:</strong> 단축 URL 서비스는 Read Heavy(보통 Read:Write = 10:1) 시스템입니다. MySQL 하나만 제안하기보다는, 빠른 리디렉션을 위해 Redis/Memcached 같은 캐시 레이어 도입이 필수적으로 언급되어야 합니다."
  },
  {
    "id": "032_sliding_window_maximum",
    "prompt": "Given an array `nums`, there is a sliding window of size `k` which is moving from the very left of the array to the very right. You can only see the `k` numbers in the window. Return the max sliding window.",
    "code": "def maxSlidingWindow(nums, k):\n    res = []\n    for i in range(len(nums) - k + 1):\n        res.append(max(nums[i:i+k]))\n    return res",
    "solution_html": "<h3>1. Logic Bug (성능 오류)</h3><br><strong>문제점:</strong> 이 코드는 논리적으로는 맞지만, 매 윈도우마다 <code>max()</code> 함수를 호출하여 O(N*k)의 시간 복잡도를 가집니다. k가 클 경우 매우 비효율적입니다.<br><strong>수정:</strong> Monotonic Deque(단조 큐)를 사용하여 각 요소를 한 번씩만 추가/제거함으로써 <strong>O(N)</strong> 시간 복잡도로 최적화해야 합니다.<br><br><h3>2. Complexity (시간 복잡도)</h3><br><strong>현재:</strong> O(N*k) - 윈도우마다 <code>max()</code> 수행.<br><strong>최적화:</strong> O(N) - Monotonic Deque 사용.<br><br><h3>3. Ideal English Feedback</h3><br>\"The model's implementation uses a naive O(N*k) approach by calling <code>max()</code> on each window. While functionally correct, this is highly inefficient for large values of <code>k</code>.<br>The optimal solution should utilize a monotonic deque to maintain indices of elements in the current window in decreasing order. This allows finding the maximum in constant time, reducing the overall time complexity to O(N).\""
  },
  {
    "id": "021_group_anagrams",
    "prompt": "\"문자열 배열을 입력받아 애너그램(Anagram) 그룹을 묶으세요.\"",
    "code": "def groupAnagrams(strs):\n    result = []\n    for s in strs:\n        # Naive approach: check if s is an anagram of any group\n        found = False\n        for group in result:\n            if sorted(s) == sorted(group[0]):\n                group.append(s)\n                found = True\n                break\n        if not found:\n            result.append([s])\n    return result",
    "solution_html": "<h3>1. Character Count Optimization</h3><br><strong>문제점:</strong> 문자열을 매번 정렬하는 것은 KlogK 복잡도를 가집니다.<br><strong>수정:</strong> 문자열을 정렬하는 대신, 각 문자의 빈도수(Character Count)를 튜플이나 문자열로 변환하여 해시맵의 키로 사용하면 O(N * K) 복잡도로 최적화할 수 있습니다.<br><br><h3>2. Handling Special Characters</h3><br><strong>수정:</strong> 입력 문자열에 공백이나 특수 문자가 포함될 경우에 대한 처리도 함께 고려되어야 합니다."
  },
  {
    "id": "006_top_k_frequent_elements",
    "prompt": "\"정수 배열 `nums`와 정수 `k`가 주어질 때, 가장 자주 등장하는 `k`개의 요소를 반환하는 파이썬 함수를 작성해줘. 시간 복잡도는 O(N log N)보다 좋아야 해.\"",
    "code": "def topKFrequent(nums, k):\n    counts = {}\n    for num in nums:\n        if num not in counts:\n            counts[num] = 0\n        counts[num] += 1\n    \n    # Sort the dictionary keys by their frequencies in descending order\n    sorted_keys = sorted(counts.keys(), key=lambda x: counts[x], reverse=True)\n    \n    # Return the first k elements\n    return sorted_keys[:k]",
    "solution_html": "<h3>1. Complexity (제약 조건 위반)</h3><br><strong>문제점:</strong> 프롬프트에서 \"시간 복잡도는 O(N log N)보다 좋아야 해\"라고 명시했습니다. 모델은 딕셔너리를 정렬(<code>sorted()</code>)하는 방식을 사용했는데, 파이썬의 <code>sorted()</code>는 TimSort를 사용하므로 배열의 고유 원소 개수 M에 대해 <strong>O(M log M)</strong>의 시간 복잡도를 가집니다. 만약 M이 N에 수렴한다면 이는 O(N log N)이 되어 제약 조건을 위반합니다.<br><strong>수정:</strong> Python의 <code>collections.Counter</code>와 <code>heapq</code> (Min-Heap 유지)를 결합하거나, <strong>Bucket Sort</strong> 방식을 사용하여 시간 복잡도를 <strong>O(N log k)</strong> 또는 <strong>O(N)</strong>으로 최적화해야 합니다.<br><br><h3>2. Edge Case (단일 요소 등)</h3><br><strong>문제점:</strong> 기능적으로는 정답을 도출하지만, 제약사항 위반 및 파이썬의 내장 모듈 활용 능력 측면에서 모범 답안으로 보기는 어렵습니다.<br><strong>수정:</strong> <code>Counter(nums).most_common(k)</code>를 사용하는 것이 파이썬의 모범 사례이나, 내부적으로 정렬이 일어날 수 있으므로 알고리즘 인터뷰 관점에서는 Bucket Sort를 직접 구현하는 것이 완벽한 O(N) 해결책입니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"While the model's code produces the correct output, it completely ignores the explicit constraint in the prompt: 'Your algorithm's time complexity must be better than O(N log N)'. The model uses Python's built-in <code>sorted()</code> function, which operates in O(M log M) time (where M is the number of unique elements). In the worst case, this approaches O(N log N). To satisfy the constraint, the model should have implemented a Min-Heap (time complexity O(N log k)) or Bucket Sort (time complexity O(N)).\""
  },
  {
    "id": "011_memoization_mutable_default",
    "prompt": "\"피보나치 수열을 메모이제이션을 활용해 구현하세요.\"",
    "code": "def fib(n, memo={}):\n    if n in memo:\n        return memo[n]\n    if n <= 1:\n        return n\n    memo[n] = fib(n-1) + fib(n-2)\n    return memo[n]",
    "solution_html": "<h3>1. Python Specific Bug (Mutable Default Argument)</h3><br><strong>문제점:</strong> <code>memo={}</code> 와 같이 Mutable 객체를 기본 인자로 설정하면, 함수가 정의될 때 한 번만 인스턴스화됩니다. 따라서 여러 번 <code>fib()</code>를 독립적으로 호출할 때 이전 호출의 결과가 <code>memo</code>에 계속 남아있게 되어 예상치 못한 부작용이나 메모리 릭이 발생할 수 있습니다.<br><strong>수정:</strong> <code>memo=None</code>으로 설정하고, 함수 내부에서 <code>if memo is None: memo = {}</code>로 초기화해야 합니다."
  },
  {
    "id": "001_two_sum_edge_case",
    "prompt": "\"정수 배열 `nums`와 정수 `target`이 주어집니다. 배열 안의 두 숫자를 더해서 `target`이 되는 두 숫자의 '인덱스'를 배열 형태로 반환하는 파이썬 함수를 만들어줘. 정답은 무조건 1개만 존재한다고 가정해.\"",
    "code": "def two_sum(nums, target):\n    # Iterate through each element in the array\n    for i in range(len(nums)):\n        for j in range(len(nums)):\n            # Check if the sum equals the target\n            if nums[i] + nums[j] == target:\n                return [i, j]",
    "solution_html": "<h3>1. Logic Bug (치명적인 논리 오류)</h3><br><strong>문제점:</strong> 이 코드는 <code>i</code>와 <code>j</code>가 같은 값을 가질 수 있도록 허용합니다. 만약 <code>nums = [3, 2, 4]</code> 이고 <code>target = 6</code>이라면, 이 코드는 요소 3을 두 번 더해서 (3+3=6) <code>[0, 0]</code>을 반환하고 실패합니다.<br><strong>수정:</strong> 내부 루프는 <code>for j in range(i + 1, len(nums)):</code> 로 변경되어야 합니다.<br><br><h3>2. Complexity (시간 복잡도)</h3><br><strong>현재:</strong> 이중 for문을 사용하므로 <strong>O(N²)</strong> Time Complexity를 가집니다.<br><strong>최적화:</strong> <strong>Hash Map (Dictionary)</strong>를 사용하여 한 번의 순회로 해결할 수 있습니다. <code>target - nums[i]</code> 값이 딕셔너리에 존재하는지 확인하면 <strong>O(N)</strong>으로 줄일 수 있습니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The model's code fails due to a critical logic bug: it allows using the same element twice because the inner loop starts at 0 instead of <code>i + 1</code>. If <code>nums = [3, 2, 4]</code> and <code>target = 6</code>, it incorrectly returns <code>[0, 0]</code>.<br>Additionally, the solution is highly inefficient with an O(N²) time complexity. It should be optimized using a Hash Map to store previously seen values and their indices, which would reduce the time complexity to O(N).\""
  },
  {
    "id": "014_sql_window_function_bug",
    "prompt": "\"직원(Employee) 테이블에서 부서(department_id)별로 가장 높은 급여(salary)를 받는 직원의 이름과 급여를 구하는 SQL 쿼리를 작성해줘.\"",
    "code": "```sql\nSELECT name, MAX(salary)\nFROM Employee\nGROUP BY department_id;\n```",
    "solution_html": "<h3>1. SQL Standard Violation (GROUP BY 에러)</h3><br><strong>문제점:</strong> 대부분의 모던 RDBMS(PostgreSQL, SQL Server 등)에서는 <code>GROUP BY</code> 절에 포함되지 않은 일반 컬럼(<code>name</code>)을 SELECT 절에서 집계 함수 없이 사용할 수 없습니다. (MySQL의 특정 설정에서는 동작할 수 있으나 표준이 아닙니다.)<br><strong>수정:</strong> <code>RANK()</code> 또는 <code>ROW_NUMBER()</code> 같은 윈도우 함수(Window Function)를 사용하거나 서브쿼리 조인을 통해 해결해야 합니다.<br><br><h3>2. Ideal English Feedback</h3><br>\"The generated SQL is invalid in standard RDBMS like PostgreSQL or SQL Server. You cannot select the <code>name</code> column without including it in the <code>GROUP BY</code> clause or wrapping it in an aggregate function. To find the employee with the highest salary per department, you should use a window function like <code>RANK() OVER (PARTITION BY department_id ORDER BY salary DESC)</code> or use a subquery to join the max salaries back to the original table.\""
  },
  {
    "id": "026_binary_search_overflow",
    "prompt": "Implement binary search. \nMake sure the midpoint calculation prevents integer overflow \n(e.g., in languages with limits, though Python handles arbitrarily large ints, \nsimulate the safe mid calculation left + (right - left) // 2).",
    "code": "class Solution:\n    def search(self, nums: List[int], target: int) -> int:\n        left, right = 0, len(nums) - 1\n        while left <= right:\n            mid = (left + right) // 2\n            if nums[mid] == target:\n                return mid\n            elif nums[mid] < target:\n                left = mid + 1\n            else:\n                right = mid - 1\n        return -1",
    "solution_html": "<code></code>`python<br>class Solution:<br>    def search(self, nums: List[int], target: int) -> int:<br>        left, right = 0, len(nums) - 1<br>        while left <= right:<br>            mid = left + (right - left) // 2<br>            if nums[mid] == target:<br>                return mid<br>            elif nums[mid] < target:<br>                left = mid + 1<br>            else:<br>                right = mid - 1<br>        return -1<br><code></code>`"
  },
  {
    "id": "012_rate_limiter_race_condition",
    "prompt": "\"Redis를 사용하는 간단한 API Rate Limiter를 작성해주세요. 사용자 ID당 분당 100회로 제한합니다.\"",
    "code": "def is_allowed(user_id, redis_client):\n    key = f\"rate_limit:{user_id}\"\n    requests = redis_client.get(key)\n    if requests is None:\n        redis_client.set(key, 1, ex=60)\n        return True\n    if int(requests) >= 100:\n        return False\n    redis_client.incr(key)\n    return True",
    "solution_html": "<h3>1. Race Condition (동시성 오류)</h3><br><strong>문제점:</strong> <code>.get()</code> 과 <code>.incr()</code> 사이에 원자성(Atomicity)이 보장되지 않습니다. 두 스레드가 동시에 <code>get</code>을 호출하여 99를 읽으면 둘 다 <code>incr</code>을 실행해 실제로는 101번의 요청이 허용될 수 있습니다.<br><strong>수정:</strong> Redis의 <code>INCR</code> 명령어를 먼저 사용하고, 반환값이 1일 때 <code>EXPIRE</code>를 설정하는 방식이 원자적이며 안전합니다. 또는 Lua 스크립트를 사용해야 합니다."
  },
  {
    "id": "029_dfs_visit_states",
    "prompt": "Return true if you can finish all courses, else false.\nThis is a cycle detection problem in a directed graph.",
    "code": "class Solution:\n    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:\n        graph = [[] for _ in range(numCourses)]\n        for dest, src in prerequisites:\n            graph[src].append(dest)\n        \n        visited = [False] * numCourses\n        \n        def dfs(node):\n            if visited[node]:\n                return True\n            visited[node] = True\n            for neighbor in graph[node]:\n                if dfs(neighbor):\n                    return True\n            visited[node] = False\n            return False\n        \n        for i in range(numCourses):\n            if dfs(i):\n                return False\n        return True",
    "solution_html": "<code></code>`python<br>class Solution:<br>    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:<br>        graph = [[] for _ in range(numCourses)]<br>        for dest, src in prerequisites:<br>            graph[src].append(dest)<br>        <br>        # 0: unvisited, 1: visiting, 2: visited<br>        state = [0] * numCourses<br>        <br>        def has_cycle(node):<br>            if state[node] == 1:<br>                return True<br>            if state[node] == 2:<br>                return False<br>            <br>            state[node] = 1<br>            for neighbor in graph[node]:<br>                if has_cycle(neighbor):<br>                    return True<br>            state[node] = 2<br>            return False<br>            <br>        for i in range(numCourses):<br>            if has_cycle(i):<br>                return False<br>        return True<br><code></code>`"
  },
  {
    "id": "018_sysdesign_url_shortener",
    "prompt": "\"URL 단축 서비스를 설계해주세요. 긴 URL을 받아서 짧은 URL로 변환하고, 사용자가 짧은 URL에 접속하면 원래의 긴 URL로 리다이렉트되어야 합니다.\"",
    "code": "(모델의 답변 요약)\n\"데이터베이스의 기본 키를 기반으로 URL을 해싱하여 짧은 문자열을 만듭니다. MD5 해싱 알고리즘을 사용하고, 해시된 문자열을 DB의 짧은 URL 컬럼에 저장합니다. 모든 검색은 DB 인덱스를 통해 수행됩니다.\"",
    "solution_html": "<h3>1. Collisions with Hashing</h3><br><strong>문제점:</strong> MD5와 같은 알고리즘을 단순 사용하면 대규모 서비스에서 해시 충돌(Collision)이 빈번하게 발생할 수 있습니다.<br><strong>수정:</strong> <strong>Base62 인코딩</strong>과 결합된 분산 ID 생성 서비스(예: Snowflake)를 사용하여 고유성을 보장해야 합니다.<br><br><h3>2. Read-Heavy Optimization</h3><br><strong>문제점:</strong> 리다이렉트(Read)가 URL 단축(Write)보다 훨씬 빈번한 Read-Heavy 환경입니다.<br><strong>수정:</strong> <strong>Redis</strong>와 같은 캐싱 레이어를 필수로 도입하여, DB를 직접 조회하지 않고도 즉시 URL을 반환할 수 있도록 설계해야 합니다."
  },
  {
    "id": "024_merge_sorted_array",
    "prompt": "\"두 개의 정렬된 정수 배열 `nums1`과 `nums2`가 주어집니다. `nums1`의 크기는 `m+n`이고 `nums2`의 크기는 `n`입니다. `nums1`과 `nums2`를 합쳐서 `nums1` 하나로 정렬된 상태로 만들어줘. 추가 배열을 생성하지 말고 `nums1`을 직접 수정(in-place)해야 해.\"",
    "code": "def merge(nums1, m, nums2, n):\n    # 단순히 nums2를 nums1 뒤에 붙이고 정렬하는 방식\n    for i in range(n):\n        nums1[m + i] = nums2[i]\n    nums1.sort()",
    "solution_html": "<h3>1. Complexity (시간 복잡도)</h3><br><strong>현재:</strong> <code>nums1.sort()</code>를 사용하므로 <strong>O((M+N)log(M+N))</strong> Time Complexity를 가집니다.<br><strong>최적화:</strong> 두 배열이 이미 정렬되어 있다는 점을 활용하여 뒤에서부터 채워 넣는 3-pointer 방식을 사용하면 <strong>O(M+N)</strong> Time Complexity로 해결할 수 있습니다.<br><br><h3>2. Implementation (구현 방법)</h3><br>1. <code>nums1</code>의 마지막 유효 요소(<code>p1 = m - 1</code>)와 <code>nums2</code>의 마지막 요소(<code>p2 = n - 1</code>)를 가리키는 포인터를 만듭니다.<br>2. <code>nums1</code>의 마지막 위치(<code>p = m + n - 1</code>)부터 두 포인터가 가리키는 값을 비교하여 더 큰 값을 채워 넣습니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The current solution uses <code>nums1.sort()</code>, which is inefficient with a time complexity of O((M+N)log(M+N)). Since both arrays are already sorted, you can achieve O(M+N) time complexity by using a three-pointer approach. Start filling <code>nums1</code> from the end (index <code>m+n-1</code>) by comparing elements from the end of <code>nums1</code> and <code>nums2</code> and moving backwards. This avoids unnecessary sorting and extra space.\""
  },
  {
    "id": "002_valid_palindrome",
    "prompt": "\"주어진 문자열이 팰린드롬(앞앞으로 읽으나 뒤로 읽으나 같은 문장)인지 확인하는 파이썬 함수를 작성해줘. 영숫자(alphanumeric)만 고려하고 대소문자는 무시해야 해.\"",
    "code": "def is_palindrome(s):\n    cleaned = \"\"\n    # 영숫자만 필터링하여 소문자로 변환\n    for char in s:\n        if char.isalnum():\n            cleaned += char.lower()\n            \n    # 뒤집어서 같은지 확인\n    return cleaned == cleaned[::-1]",
    "solution_html": "<h3>1. Logic Bug / Edge Case</h3><br><strong>문제점:</strong> 기능 자체는 작동하고 엣지 케이스(빈 문자열)도 통과하지만, 완전히 비어있거나 특수문자만 있는 문자열(<code>\" ,. \"</code>)이 들어올 경우 <code>cleaned</code>가 빈 문자열 <code>\"\"</code>이 되어 무조건 <code>True</code>를 반환합니다. (문제의 정의에 따라 방어해야 할 수도 있음)<br><br><h3>2. Complexity & Optimization</h3><br><strong>현재:</strong> 시간 복잡도는 <strong>O(N)</strong>, 공간 복잡도는 새로운 문자열 <code>cleaned</code>를 생성하므로 <strong>O(N)</strong> 입니다. 파이썬에서 문자열은 불변(immutable)이므로 <code>+=</code> 연산은 매번 새로운 문자열을 할당하여 메모리 낭비가 큽니다.<br><strong>최적화:</strong> 새로운 문자열을 만들지 않고, <strong>Two Pointers (투 포인터)</strong> 방식을 사용하여 문자열 양 끝에서 중앙으로 이동하며 비교하면 공간 복잡도를 <strong>O(1)</strong>로 줄일 수 있습니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"While the logic correctly identifies palindromes, it is highly inefficient in terms of space complexity. By creating a new <code>cleaned</code> string using the <code>+=</code> operator, the spatial complexity becomes O(N), which is costly since strings are immutable in Python. <br><br>I recommend optimizing this using a 'Two Pointers' approach. By placing one pointer at the start and another at the end of the string, you can iterate inwards, skipping non-alphanumeric characters, and compare them in place. This reduces the space complexity to O(1).\""
  },
  {
    "id": "004_merge_intervals",
    "prompt": "\"겹치는 구간들을 병합하는 함수를 짜줘. 구간은 리스트의 리스트 형태로 들어와. 예를 들어 `[[1,3],[2,6],[8,10],[15,18]]`가 들어오면 `[[1,6],[8,10],[15,18]]`을 반환해야 해.\"",
    "code": "def merge_intervals(intervals):\n    if not intervals:\n        return []\n        \n    merged = []\n    # 첫 번째 구간을 넣고 시작\n    merged.append(intervals[0])\n    \n    for i in range(1, len(intervals)):\n        last_merged = merged[-1]\n        current = intervals[i]\n        \n        # 겹치는지 확인\n        if current[0] <= last_merged[1]:\n            last_merged[1] = max(last_merged[1], current[1])\n        else:\n            merged.append(current)\n            \n    return merged",
    "solution_html": "<h3>1. Logic Bug / Edge Case</h3><br><strong>문제점:</strong> 배열이 <strong>정렬되어 있다는 보장이 없을 때</strong> 치명적으로 실패합니다. 입력이 <code>[[1,4], [4,5]]</code> 처럼 정렬되어 있다면 작동하지만, <code>[[1,4], [0,4]]</code> 처럼 무작위 순서로 들어오면 겹치는 구간을 병합하지 못하고 그대로 반환하게 됩니다.<br><strong>수정:</strong> 알고리즘을 시작하기 전에 <code>intervals.sort(key=lambda x: x[0])</code> 를 사용하여 시작 시간을 기준으로 구간을 정렬해야 합니다.<br><br><h3>2. Complexity & Optimization</h3><br><strong>현재:</strong> 시간 복잡도는 <strong>O(N)</strong> 이지만 정렬되지 않은 입력에 대해 오답을 냅니다.<br><strong>최적화:</strong> 위에서 지적한 대로 O(N log N) 정렬 과정이 필수적으로 선행되어야 합니다. 정렬 후 순회하면 완벽합니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The logic correctly merges overlapping intervals, and I appreciate the guard clause <code>if not intervals: return []</code> handling the empty array edge case. <br><br>However, the code assumes that the <code>intervals</code> array is already sorted. If an unsorted array like <code>[[1,4], [0,4]]</code> is passed, the function will fail to merge them correctly. <br><br>You need to sort the array based on the start time of each interval before starting the iteration. Adding <code>intervals.sort(key=lambda x: x[0])</code> at the beginning of the function will resolve this issue, resulting in an overall time complexity of O(N log N).\""
  },
  {
    "id": "019_binary_search_overflow",
    "prompt": "\"이진 탐색(Binary Search) 알고리즘을 구현하세요. 정렬된 배열에서 타겟 값을 찾으면 인덱스를 반환하고, 없으면 -1을 반환합니다.\"",
    "code": "def binary_search(arr, target):\n    low = 0\n    high = len(arr) - 1\n    while low <= high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1",
    "solution_html": "<h3>1. Integer Overflow</h3><br><strong>문제점:</strong> <code>mid = (low + high) // 2</code> 는 <code>low + high</code>가 매우 클 경우 정수 오버플로우가 발생할 수 있습니다 (언어에 따라).<br><strong>수정:</strong> <code>mid = low + (high - low) // 2</code> 로 변경하여 오버플로우를 예방해야 합니다."
  },
  {
    "id": "017_sysdesign_chat_app",
    "prompt": "\"WhatsApp과 같은 실시간 채팅 앱의 백엔드를 설계해주세요. 사용자는 메시지를 보내고 지연 없이 수신해야 합니다.\"",
    "code": "(모델의 답변 요약)\n\"사용자는 REST API 엔드포인트 `POST /messages`를 통해 메시지를 서버에 보냅니다. 다른 사용자들은 매초마다 `GET /messages`를 호출하는 HTTP Polling 방식을 사용하여 새로운 메시지가 있는지 확인하고 화면을 업데이트합니다.\"",
    "solution_html": "<h3>1. Protocol Inefficiency (HTTP Polling)</h3><br><strong>문제점:</strong> 매초 수백만 명의 사용자가 HTTP Polling을 수행하면 서버 트래픽과 리소스가 낭비되고 서버는 곧 다운됩니다. 실시간 채팅 앱에서는 절대 권장되지 않는 안티패턴입니다.<br><strong>수정:</strong> Persistent Connection을 유지하는 <strong>WebSocket</strong> 프로토콜이나 Server-Sent Events (SSE)를 사용해야 합니다. <br><h3>2. Scalability (Pub/Sub 누락)</h3><br><strong>수정:</strong> 백엔드 서버가 여러 대일 경우, 사용자 A가 접속한 서버와 사용자 B가 접속한 서버가 다를 수 있습니다. 이 경우 서버 간 메시지를 라우팅하기 위해 <strong>Redis Pub/Sub</strong>나 Kafka 같은 Message Broker 도입을 언급해야 합니다."
  },
  {
    "id": "005_lru_cache_buggy",
    "prompt": "Implement an LRU (Least Recently Used) cache. The cache should be initialized with a positive `capacity`.\n`get(key)`: Return the value of the key if the key exists, otherwise return -1.\n`put(key, value)`: Update the value of the key if the key exists. Otherwise, add the key-value pair to the cache. If the number of keys exceeds the capacity from this operation, evict the least recently used key.\n\nThe functions `get` and `put` must each run in O(1) average time complexity.",
    "code": "class LRUCache:\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self.cache = {}\n        self.usage_history = []\n\n    def get(self, key: int) -> int:\n        if key in self.cache:\n            # Update history: remove key and append to the end\n            self.usage_history.remove(key)\n            self.usage_history.append(key)\n            return self.cache[key]\n        return -1\n\n    def put(self, key: int, value: int) -> None:\n        if key in self.cache:\n            self.cache[key] = value\n            self.usage_history.remove(key)\n            self.usage_history.append(key)\n        else:\n            if len(self.cache) >= self.capacity:\n                # Evict the least recently used key\n                lru_key = self.usage_history.pop(0)\n                del self.cache[lru_key]\n            \n            self.cache[key] = value\n            self.usage_history.append(key)",
    "solution_html": "## Flaw Identification<br>The generated code implements the LRU Cache using a dictionary <code>self.cache</code> and a Python list <code>self.usage_history</code>. While functionally correct for maintaining the LRU order, it heavily violates the O(1) time complexity requirement specified in the prompt.<br><br>1. <strong>O(N) Time Complexity in <code>remove</code></strong>: Python's list <code>remove(key)</code> method runs in O(N) time because it has to scan the list to find the element and then shift all subsequent elements. This happens in both <code>get</code> and <code>put</code>.<br>2. <strong>O(N) Time Complexity in <code>pop(0)</code></strong>: Removing the first element of a list using <code>pop(0)</code> also takes O(N) time because it requires shifting all remaining elements to the left.<br><br>## Suggested Fix<br>To achieve O(1) time complexity for both operations, the cache must be implemented using a combination of a Hash Map and a Doubly Linked List. Alternatively, in Python 3.7+, <code>collections.OrderedDict</code> can be used as it is implemented via a doubly linked list under the hood and preserves insertion order natively in O(1) time.<br><br><code></code>`python<br>from collections import OrderedDict<br><br>class LRUCache:<br>    def __init__(self, capacity: int):<br>        self.capacity = capacity<br>        self.cache = OrderedDict()<br><br>    def get(self, key: int) -> int:<br>        if key not in self.cache:<br>            return -1<br>        # Move the accessed item to the end to mark it as recently used<br>        self.cache.move_to_end(key)<br>        return self.cache[key]<br><br>    def put(self, key: int, value: int) -> None:<br>        if key in self.cache:<br>            # Update the value and mark as recently used<br>            self.cache.move_to_end(key)<br>        self.cache[key] = value<br>        <br>        # Evict if capacity exceeded<br>        if len(self.cache) > self.capacity:<br>            self.cache.popitem(last=False) # popitem(last=False) pops in FIFO order<br><code></code>`"
  },
  {
    "id": "009_coin_change_greedy_bug",
    "prompt": "\"주어진 동전 단위 배열 `coins`를 사용하여 목표 금액 `amount`를 만들 수 있는 최소 동전 개수를 구하세요.\"",
    "code": "def coinChange(coins, amount):\n    coins.sort(reverse=True)\n    count = 0\n    for coin in coins:\n        count += amount // coin\n        amount %= coin\n    return count if amount == 0 else -1",
    "solution_html": "<h3>1. Logic Bug (Greedy Fallacy)</h3><br><strong>문제점:</strong> 모델은 그리디(Greedy) 접근법을 사용했습니다. 큰 동전부터 최대한 많이 사용하는 방식입니다. 그러나 동전 시스템이 배수 관계가 아닐 경우(예: <code>coins = [1, 3, 4]</code>, <code>amount = 6</code>) 오답을 반환합니다. 그리디는 4를 하나 고르고 1을 두 개 골라 총 3개를 반환하지만, 실제 정답은 3을 두 개 고르는 2개입니다.<br><strong>수정:</strong> DP (Dynamic Programming) 또는 BFS 최단 거리 탐색 방식으로 해결해야 합니다."
  },
  {
    "id": "003_binary_search_loop",
    "prompt": "\"정렬된 정수 배열 `nums`와 찾고자 하는 값 `target`이 주어집니다. `target`이 배열에 존재하면 해당 인덱스를, 없으면 -1을 반환하는 Binary Search (이진 탐색) 함수를 작성해줘.\"",
    "code": "def binary_search(nums, target):\n    left = 0\n    right = len(nums) - 1\n    \n    while left <= right:\n        mid = (left + right) // 2\n        \n        if nums[mid] == target:\n            return mid\n        elif nums[mid] < target:\n            left = mid\n        else:\n            right = mid\n            \n    return -1",
    "solution_html": "<h3>1. Logic Bug / Edge Case</h3><br><strong>문제점:</strong> 치명적인 <strong>무한 루프(Infinite Loop)</strong> 버그가 있습니다. <code>target</code>을 찾지 못해 범위를 좁혀야 할 때, <code>left = mid</code> 또는 <code>right = mid</code>로 업데이트하면 범위가 1 이하로 좁혀졌을 때 <code>mid</code>가 계속 제자리에 머물게 됩니다. (예: <code>left=0</code>, <code>right=1</code> 일 때 <code>mid=0</code>이 반복됨)<br><strong>수정:</strong> <code>left = mid + 1</code>, <code>right = mid - 1</code> 로 변경하여 탐색한 <code>mid</code>를 다음 범위에서 완전히 제외해야 합니다.<br><br><h3>2. Complexity & Optimization</h3><br><strong>현재:</strong> <code>while</code> 문 로직 수정 후에는 시간 복잡도 <strong>O(log N)</strong>, 공간 복잡도 <strong>O(1)</strong> 로 최적의 상태입니다. (단, Python 외 언어에서는 <code>mid = left + (right - left) // 2</code> 를 사용하여 Integer Overflow를 방지해야 하지만 파이썬은 자동으로 큰 정수를 지원하므로 생략 가능합니다.)<br><br><h3>3. Ideal English Feedback</h3><br>\"The generated code contains a critical logic error that results in an infinite loop. Inside the <code>while</code> loop, you update the pointers as <code>left = mid</code> and <code>right = mid</code>. When the search space narrows down to two adjacent elements, <code>mid</code> will repeatedly evaluate to <code>left</code>, causing the loop to never terminate.<br><br>To fix this, you must exclude the already checked <code>mid</code> element from the next search space. Update the pointers to <code>left = mid + 1</code> and <code>right = mid - 1</code> respectively.\""
  },
  {
    "id": "008_course_schedule",
    "prompt": "\"수강해야 하는 총 코스 수 `numCourses`와 선수 과목 쌍 배열 `prerequisites`가 주어집니다. `prerequisites[i] = [a, b]`는 코스 `a`를 듣기 위해서는 코스 `b`를 먼저 들어야 한다는 뜻입니다. 모든 코스를 수강할 수 있으면 `true`, 아니면 `false`를 반환하는 파이썬 함수를 만들어줘.\"",
    "code": "def canFinish(numCourses, prerequisites):\n    graph = {i: [] for i in range(numCourses)}\n    for course, pre in prerequisites:\n        graph[course].append(pre)\n        \n    visited = set()\n    \n    def dfs(course):\n        if course in visited:\n            return False\n        \n        if not graph[course]:\n            return True\n            \n        visited.add(course)\n        for pre in graph[course]:\n            if not dfs(pre):\n                return False\n        visited.remove(course)\n        graph[course] = []\n        return True\n        \n    for i in range(numCourses):\n        if not dfs(i):\n            return False\n            \n    return True",
    "solution_html": "<h3>1. Performance / Time Limit Exceeded (중복 방문)</h3><br><strong>문제점:</strong> 모델은 DFS를 이용해 사이클(Cycle)을 탐지하려고 했습니다. 그러나 <code>visited.remove(course)</code>를 호출하는 방식은 백트래킹(Backtracking)이지, <strong>위상 정렬(Topological Sort)</strong>이나 그래프 사이클 검출을 위한 올바른 상태 추적이 아닙니다. 이 방식은 이미 성공적으로 검증된 경로를 다른 노드에서 재방문할 때 다시 연산하게 만들어 <strong>O(V * E)</strong> 또는 최악의 경우 지수 시간복잡도를 유발하여 Time Limit Exceeded (TLE)가 발생할 수 있습니다.<br><strong>수정:</strong> 노드의 상태를 3가지로 나누어야 합니다: <code>0</code> (미방문), <code>1</code> (방문 중 - 현재 탐색 경로에 있음), <code>2</code> (방문 완료 - 사이클 없음이 확정됨). 상태를 추적하는 배열이나 딕셔너리를 사용하여 중복 탐색을 막아야 합니다.<br><br><h3>2. Logic (부분적인 불완전성)</h3><br><strong>문제점:</strong> 이 코드는 운 좋게도 그래프가 트리 형태에 가깝고 작을 때는 동작하며, <code>graph[course] = []</code> 처리로 일부 중복 방문을 막으려 한 흔적이 보입니다. 그러나 완전히 방문이 끝난 노드(안전한 노드)를 표시하는 명시적 방법(Memoization)이 없어 불완전합니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The model's DFS implementation for cycle detection is inefficient and prone to a Time Limit Exceeded (TLE) error on large, complex graphs. The use of a single <code>visited</code> set where nodes are added and then removed (<code>visited.remove(course)</code>) acts like backtracking, meaning nodes might be redundantly evaluated multiple times across different paths. Although the model attempts a small optimization by clearing the adjacency list (<code>graph[course] = []</code>), a proper topological sort via DFS requires keeping track of three distinct states: unvisited, currently visiting (to detect cycles), and fully visited/safe (to avoid redundant checks). The time complexity should be strictly O(V + E).\""
  },
  {
    "id": "027_kadanes_negative",
    "prompt": "Implement Kadane's Algorithm to find the maximum subarray sum.\nThe algorithm should handle cases where all numbers are negative.",
    "code": "class Solution:\n    def maxSubArray(self, nums: List[int]) -> int:\n        max_sum = 0\n        current_sum = 0\n        for num in nums:\n            current_sum += num\n            if current_sum < 0:\n                current_sum = 0\n            if current_sum > max_sum:\n                max_sum = current_sum\n        return max_sum",
    "solution_html": "<code></code>`python<br>class Solution:<br>    def maxSubArray(self, nums: List[int]) -> int:<br>        max_sum = nums[0]<br>        current_sum = nums[0]<br>        for num in nums[1:]:<br>            current_sum = max(num, current_sum + num)<br>            max_sum = max(max_sum, current_sum)<br>        return max_sum<br><code></code>`"
  },
  {
    "id": "031_3sum",
    "prompt": "Given an integer array `nums`, return all the triplets `[nums[i], nums[j], nums[k]]` such that `i != j`, `i != k`, and `j != k`, and `nums[i] + nums[j] + nums[k] == 0`.",
    "code": "def three_sum(nums):\n    res = []\n    for i in range(len(nums)):\n        for j in range(i + 1, len(nums)):\n            for k in range(j + 1, len(nums)):\n                if nums[i] + nums[j] + nums[k] == 0:\n                    res.append([nums[i], nums[j], nums[k]])\n    return res",
    "solution_html": "<h3>1. Logic Bug (치명적인 논리 오류)</h3><br><strong>문제점:</strong> 이 코드는 중복된 트리플렛을 제거하지 않습니다. 예: <code>[-1, 0, 1, 2, -1, -4]</code> 일 때 <code>[-1, 0, 1]</code>이 여러 번 나올 수 있습니다.<br><strong>수정:</strong> 정렬 후 Two Pointers 기법을 사용하거나, 결과 세트를 <code>set</code>으로 처리해야 합니다.<br><br><h3>2. Complexity (시간 복잡도)</h3><br><strong>현재:</strong> 3중 for문을 사용하므로 <strong>O(N³)</strong> Time Complexity를 가집니다.<br><strong>최적화:</strong> 정렬 후 Two Pointers를 사용하여 <strong>O(N²)</strong>로 줄일 수 있습니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The model's code is highly inefficient, using a triple-nested loop which results in O(N³) time complexity. It also fails to handle duplicate triplets, which is a key requirement of the 3Sum problem.<br>The solution should first sort the array and then use a fixed element with a two-pointer approach to find the remaining two numbers. This optimized approach reduces the time complexity to O(N²), and adding checks for duplicates ensures unique results are returned.\""
  },
  {
    "id": "013_pandas_chained_assignment",
    "prompt": "\"Pandas를 사용하여 데이터프레임 `df`에서 'age'가 30 이상인 사람들의 'status'를 'senior'로 변경하세요.\"",
    "code": "import pandas as pd\n\ndef update_status(df):\n    seniors = df[df['age'] >= 30]\n    seniors['status'] = 'senior'\n    return df",
    "solution_html": "<h3>1. Library Misuse (SettingWithCopyWarning)</h3><br><strong>문제점:</strong> <code>df[df['age'] >= 30]</code>은 뷰(View)가 아닌 복사본(Copy)을 반환할 수 있습니다. <code>seniors['status'] = ...</code>는 Chained Assignment를 발생시켜 원본 <code>df</code>에 반영되지 않고 경고를 발생시킵니다.<br><strong>수정:</strong> <code>.loc</code>를 사용하여 원본 데이터를 직접 수정해야 합니다. <code>df.loc[df['age'] >= 30, 'status'] = 'senior'</code>"
  },
  {
    "id": "020_longest_substring_without_repeating_characters",
    "prompt": "\"문자열 내에서 반복되지 않는 가장 긴 부분 문자열의 길이를 찾으세요.\"",
    "code": "(모델의 답변 요약)\n\"모든 부분 문자열을 생성하고, 각 부분 문자열이 반복되는지 확인하여 가장 긴 것을 고릅니다. 이 과정은 중첩 반복문을 사용하여 O(N^2) 이상의 복잡도를 가집니다.\"",
    "solution_html": "<h3>1. Time Complexity (Nested Loops)</h3><br><strong>문제점:</strong> 중첩 반복문을 사용하는 방식은 비효율적입니다.<br><strong>수정:</strong> <strong>Sliding Window</strong> 기법과 해시맵(HashMap)을 활용하여 O(N) 복잡도로 해결해야 합니다.<br><br><h3>2. HashMap for Sliding Window</h3><br><strong>수정:</strong> 해시맵에 각 문자의 가장 최근 인덱스를 저장하여, 반복되는 문자가 나오면 윈도우의 시작 위치를 효율적으로 갱신합니다."
  },
  {
    "id": "007_max_subarray",
    "prompt": "\"정수 배열 `nums`가 주어졌을 때, 연속된 하위 배열(최소 하나의 숫자 포함)의 합이 가장 큰 값을 찾아 그 합을 반환하는 파이썬 함수를 만들어줘.\"",
    "code": "def maxSubArray(nums):\n    max_sum = 0\n    current_sum = 0\n    \n    for num in nums:\n        current_sum += num\n        if current_sum < 0:\n            current_sum = 0\n        if current_sum > max_sum:\n            max_sum = current_sum\n            \n    return max_sum",
    "solution_html": "<h3>1. Logic Bug (음수 엣지 케이스 실패)</h3><br><strong>문제점:</strong> 배열의 모든 원소가 음수인 경우 (예: <code>[-3, -5, -2]</code>), 이 알고리즘은 0을 반환합니다. 프롬프트에서 \"최소 하나의 숫자 포함\"이라고 명시했으므로, 이 경우 정답은 <code>-2</code>가 되어야 합니다. <code>max_sum</code>을 0으로 초기화한 것이 원인입니다.<br><strong>수정:</strong> <code>max_sum</code>은 배열의 첫 번째 요소 <code>nums[0]</code> 또는 <code>-float('inf')</code>로 초기화해야 합니다.<br><br><h3>2. Algorithmic Pattern (카데인 알고리즘 오구현)</h3><br><strong>문제점:</strong> 이 코드는 Kadane's Algorithm의 기본 아이디어를 차용했으나, <code>current_sum</code>이 음수일 때 0으로 리셋하는 로직이 모든 원소가 음수일 때의 엣지 케이스를 커버하지 못하게 만들었습니다.<br><strong>수정:</strong> <code>current_sum = max(num, current_sum + num)</code> 그리고 <code>max_sum = max(max_sum, current_sum)</code> 구조로 작성하는 것이 정석적인 카데인 알고리즘 구현입니다.<br><br><h3>3. Ideal English Feedback</h3><br>\"The model's code contains a critical logic bug regarding arrays with only negative numbers. Because <code>max_sum</code> is initialized to <code>0</code> and <code>current_sum</code> is reset to <code>0</code> whenever it dips below zero, an input like <code>[-3, -5, -2]</code> will incorrectly return <code>0</code> instead of <code>-2</code> (which is the correct max subarray sum of a single element). The prompt explicitly stated 'containing at least one number'. The model should have implemented the standard Kadane's algorithm where <code>current_sum = max(num, current_sum + num)</code> and <code>max_sum</code> is initialized to <code>nums[0]</code> or <code>-float('inf')</code>.\""
  },
  {
    "id": "010_binary_search_overflow",
    "prompt": "\"정렬된 배열에서 이진 탐색(Binary Search)을 구현하세요.\"",
    "code": "def search(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        elif nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1",
    "solution_html": "<h3>1. Potential Overflow (Integer Overflow)</h3><br><strong>문제점:</strong> 파이썬은 임의 정밀도 정수를 지원하므로 이 코드는 파이썬에서는 안전하지만, 일반적인 C/C++/Java 인터뷰 관점에서는 <code>mid = (left + right) // 2</code>가 오버플로우를 유발할 수 있는 잠재적 결함입니다.<br><strong>수정:</strong> <code>mid = left + (right - left) // 2</code> 로 작성하는 것이 언어에 무관하게 안전한 모범 답안입니다."
  },
  {
    "id": "015_sql_n_plus_one_optimization",
    "prompt": "\"주문(Orders) 테이블과 고객(Customers) 테이블이 있습니다. 각 고객의 이름과 그 고객의 총 주문 금액을 구하는 쿼리를 작성해주세요. 단, 아직 주문을 한 번도 하지 않은 고객도 결과에 포함되어야 하며 주문 금액은 0으로 표시해야 합니다.\"",
    "code": "```sql\nSELECT \n    c.name, \n    SUM(o.amount) as total_amount\nFROM Customers c\nJOIN Orders o ON c.id = o.customer_id\nGROUP BY c.id, c.name;\n```",
    "solution_html": "<h3>1. Logic Bug (INNER JOIN 누락 이슈)</h3><br><strong>문제점:</strong> 모델이 <code>JOIN</code> (INNER JOIN)을 사용했습니다. 이 경우 주문을 한 번도 하지 않은 고객은 결과에서 완전히 제외됩니다. 프롬프트의 \"아직 주문을 한 번도 하지 않은 고객도 결과에 포함\" 조건을 명백히 위반했습니다.<br><strong>수정:</strong> <code>LEFT JOIN</code>을 사용해야 합니다. 또한 주문이 없는 경우 <code>SUM</code>이 <code>NULL</code>이 되므로, <code>COALESCE(SUM(o.amount), 0)</code>을 사용하여 0으로 처리해주어야 완벽합니다."
  }
];